// before testing repl.js please navigate to node_modules/jq-console/jqconsole.min.js
// and then add var jQuery = require('jQuery'); var $ = jQuery; to the top
// if you want to test the repl please uncomment the test suite then run npm t
// afterwards please comment out the test suite again as there is an error with continous integration

// ********************** BEGIN TEST SUITE ******************

// import React from 'react';
// import { expect, should } from 'chai';
// import { mount, shallow, render } from 'enzyme';
// import sinon from 'sinon';

// import Repl from '../src/client/components/Repl';

// describe('REPL Component', () => {
//   it('should have a method for sending code to the REPL service', () => {
//     expect(Repl).to.be.a('function');
//     expect(Repl.prototype.startConsole).to.be.a('function');
//     const wrapper = shallow(<Repl />);
//     expect(wrapper).to.exist;
//   })

//   it('should render the subheader component', () => {
//     const wrapper = shallow(<Repl />);
//     expect(wrapper.find('Subheader')).to.have.length(1);
//   })

// })
